#include <iostream>
#include "node.h"
#include "LinkList.h"


int main() {
    LinkList l;
    l.addNode(1);
    l.addNode(4);
    l.addNode(9);
    l.addNode(16);
    l.addNode(25);
    l.printList();



    //if (head == nullprt)
      //  head = new node(5);
    //else
    //{

    //}
}
